# Benchmark 01: Bug #8

This benchmark is an isolated pre-solution snapshot. Agents may work from
`baseline/source` and its complete `baseline-tests` corpus. The source is a
Git archive of the exact baseline commit recorded in
`baseline/metadata/baseline.json`; it is not copied from the active dirty
development worktree.

`reproduction/bug8.md` describes the externally visible symptom only. It does
not disclose the eventual solution or post-investigation test cases.
